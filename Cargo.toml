[package]
name = "zama-dapp"
version = "0.1.0"
edition = "2021"

[dependencies]
# пример: работа с Zama Concrete FHE
concrete = "1.0.0"
tokio = { version = "1", features = ["full"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
warp = "0.3"   # простой веб-сервер для API
